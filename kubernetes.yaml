apiVersion: apps/v1
kind: Deployment
metadata:
  name: hackathon-23-3
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hackathon-23-3
  template:
    metadata:
      labels:
        app: hackathon-23-3
    spec:
      containers:
        - name: hackathon-23-3
          image: hackathon-23-3:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: '1'

---
apiVersion: v1
kind: Service
metadata:
  name: hackathon-23-3-balancer
spec:
  selector:
    app: hackathon-23-3
  ports:
    - protocol: TCP
      port: 8000 # The port to expose to the outside
      targetPort: 8080 # The port the application is running on in the pods
  type: LoadBalancer
  sessionAffinity: ClientIP
